import React, { FC } from 'react';
import LoginForm from '../components/LoginForm';

const Admin: FC = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default Admin;