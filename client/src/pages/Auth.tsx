import React, { FC } from 'react';
import LoginForm from '../components/LoginForm';

const Auth: FC = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default Auth;